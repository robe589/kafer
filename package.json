{
  "name": "@jamashita/kafer",
  "version": "0.1.0",
  "repository": {
    "type": "git",
    "url": "git@github.com:jamashita/kafer.git"
  },
  "author": "jamashita <jamashita@arachnid.xyz>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "honkit build",
    "serve": "honkit serve",
    "deploy": "yarn build && cp CNAME public/CNAME && gh-pages -d public && gatsby clean",
    "update": "yarn update:upgrade && yarn update:install",
    "update:install": "yarn install && yarn upgrade",
    "update:upgrade": "ncu --upgrade"
  },
  "dependencies": {},
  "devDependencies": {
    "honkit": "^3.6.6",
    "husky": "4.3.0",
    "lint-staged": "10.3.0",
    "npm-check-updates": "9.0.1",
    "sort-package-json": "1.44.0"
  },
  "engines": {
    "node": ">= 14.5.0",
    "npm": "npm is not supported"
  },
  "engineStrict": true
}
